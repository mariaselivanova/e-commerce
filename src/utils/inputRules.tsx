export const passwordRules = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;
export const emailRules = /[a-z0-9]+@[a-z]+\.[a-z]{2,3}/;

export const emailMessage = 'Please type an email of correct format (e.g. example@gmail.com)!';
export const passwordMassage = 'Password must contain at least 8 characters, 1 uppercase letter, 1 lowercase and 1 number!';
